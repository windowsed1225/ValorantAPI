import XCTest
// adding @testable would let us access internal members, but we'd rather just test the public interface here
import ValorantAPI

final class ValorantAPITests: XCTestCase {
	func testExample() {}
}
